language: node_js
node_js:
  - "10.5"

services:
  - mysql

before_install:
  - npm install -g npm

before_script:
  - npm install
  - mysql -e 'CREATE DATABASE IF NOT EXISTS eris-boiler-test;'

script: npm run test
