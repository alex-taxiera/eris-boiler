language: node_js
node_js:
  - "10.5"

services:
  - mysql

before_install:
  - npm install -g npm

before_script:
  - npm install
  - mysql -u root -e 'CREATE DATABASE test_db'
  - DB_USER=root
  - DB_NAME=test_db

script: npm run test